<Window x:Class="TestWPFApp.MainWindow4"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWPFApp"
        mc:Ignorable="d"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Height="260" 
        Width="400"
        Title="Ресурсы">
    <Window.Resources>
        <sys:String x:Key="strWindow">Добро пожаловать в Окно Ресурсов Window!</sys:String>
    </Window.Resources>
    <DockPanel Margin="10" Name="pnlMain">
        <DockPanel.Resources>
            <sys:String x:Key="strPanel">Добро Пожаловать на Панель!</sys:String>
        </DockPanel.Resources>
        <WrapPanel DockPanel.Dock="Top" HorizontalAlignment="Center" Margin="10">
            <Button Name="btnClickMe" Click="btnClickMe_Click">Click me!</Button>
        </WrapPanel>
        <ListBox Name="lbResult" />
        <Button Content="Кнопка" MaxHeight="40" MaxWidth="80" Background="{StaticResource Brush}"/>
    </DockPanel>

</Window>
